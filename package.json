{
  "name": "skystore-desktop-container",
  "version": "0.1.0",
  "description": "Skystore Electron container for desktop application",
  "main": "main.js",
  "scripts": {
    "start": "electron-webpack dev",
    "compile": "electron-webpack",
    "build": "rm -rf dist && npm run compile && electron-builder --config electron-builder.production.yml",
    "build-snapshot": "rm -rf dist && npm run compile && electron-builder --config electron-builder.snapshot.yml"
  },
  "dependencies": {
    "source-map-support": "^0.5.4"
  },
  "devDependencies": {
    "electron": "1.8.4",
    "electron-builder": "^20.9.2",
    "electron-webpack": "^2.0.1",
    "webpack": "^4.6.0"
  },
  "electronWebpack": {
    "main": {
      "sourceDirectory": "src",
      "extraEntries": ["./src/preloaders/index.js"]
    },
    "renderer": {
      "sourceDirectory": "src/renderers"
    }
  },
  "files": [
    "*.js",
    "build",
    "node_modules"
  ]
}
